{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "enum": ["api.tenant.create"]
    },
    "ver": {
      "type": "string"
    },
    "ts": {
      "type": "string"
    },
    "params": {
      "type": "object",
      "properties": {
        "msgid": {
          "type": "string"
        }
      },
      "required": ["msgid"],
      "additionalProperties": false
    },
    "request": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "must be a string and is required"
        },
        "type": {
          "type": "string",
          "description": "must be a string and is required"
        },
        "board_id": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "minItems": 1,
          "uniqueItems": true,
          "description": "Array of board IDs"
        }
      },
      "required": ["name", "type", "board_id"],
      "additionalProperties": false
    }
  },
  "required": ["id", "ver", "ts", "params", "request"],
  "additionalProperties": false
}
